# FastAPI and web framework
fastapi==0.141.1
uvicorn[standard]==0.34.3
python-multipart==0.0.31
starlette==1.3.1

# Database
sqlalchemy==2.0.40
pymysql>=1.1.1
cryptography>=43.0.0
alembic==1.15.2

# Authentication and security
# 3.4.0 capped pyasn1 at <0.5.0, which pinned us to a version carrying four DoS
# advisories in the BER/DER decoder. 3.5.0 relaxes that to >=0.5.0.
python-jose[cryptography]==3.5.0
pyasn1>=0.6.4
passlib[bcrypt]==1.7.4
pyotp==2.9.0
qrcode==8.2
pillow>=10.3.0

# Proxmox
proxmoxer==2.3.0
requests>=2.32.4
websockets>=12.0

# VMware import
pyvmomi

# SSH and remote management
paramiko==3.5.1

# Configuration and environment
pydantic==2.11.3
pydantic-settings==2.8.1
python-dotenv==1.2.2

# YAML processing for cloud-init
pyyaml==6.0.2
defusedxml==0.7.1

# Logging
python-json-logger==2.0.7

# File handling
aiofiles==24.1.0

# Validation
email-validator==2.2.0

# Rate limiting
slowapi==0.1.9

# Testing (optional)
pytest==9.0.3
# 0.26.0 capped pytest at <9, blocking the pytest advisory fix. 1.4.0 allows <10.
pytest-asyncio==1.4.0
httpx==0.28.1
weasyprint==70.0
markdown==3.10
apscheduler>=3.10.4
geoip2==4.8.1

# AI Reports subsystem
openai>=1.35.0
